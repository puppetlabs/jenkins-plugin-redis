<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <f:entry field="redisServerUrl" title="${%Redis Server}">
    <f:textbox />
  </f:entry>
  <f:entry field="redisNamespace" title="${%Redis Namespace}">
    <f:textbox />
  </f:entry>
  <f:entry field="exportedVariables" title="${%Comma-separated variables to import}">
    <f:textbox />
  </f:entry>
</j:jelly>
